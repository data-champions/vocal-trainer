import type { ReactNode } from 'react';
import './composer-base.css';
import './composer.css';

export default function CompositoreLayout({
  children,
}: {
  children: ReactNode;
}): ReactNode {
  return children;
}
